--- a/lib/wifi/qcawificfg80211.sh
+++ b/lib/wifi/qcawificfg80211.sh
@@ -3010,8 +3010,10 @@
 			txpower=`expr $max_power - 1`
 		elif [ "$txpwr" = "min" ]; then
 			txpower=`expr $max_power - 3`
-		else
+		elif [ "$txpwr" = "max" ]; then
 			txpower="$max_power"
+		else
+			txpower="$txpwr"
 		fi
 
 		txpower="${txpower:-$vif_txpower}"
@@ -3841,18 +3841,24 @@
 	option disabled '$disable'
 	option txbf '3'
 	option ax '1'
 EOF
-	if [ $devidx = 2 ]; then
+	if [ $devidx = 0 ]; then
 		cat <<EOF
-	option bw 20
+	option bw 80
+ 	option txpwr 'max'
 EOF
 	fi
-	if [ $devidx = 0 ]; then
+	if [ $devidx = 1 ]; then
 		cat <<EOF
-	option bw 80
+ 	option txpwr 'max'
 EOF
 	fi
+	if [ $devidx = 2 ]; then
+		cat <<EOF
+	option bw 20
+EOF
+	fi
 	cat <<EOF
 
 config wifi-iface
 	option device	wifi$devidx
--- a/usr/lib/lua/luci/view/web/apsetting/wifi.htm
+++ b/usr/lib/lua/luci/view/web/apsetting/wifi.htm
@@ -174,6 +174,9 @@
                 <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                 <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                 <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                {for (var i=0; i<=30; i++)}
+                <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                {/for}
             </select>
         </span>
         <em class="t"></em>
@@ -223,6 +226,9 @@
                 <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                 <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                 <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                {for (var i=0; i<=30; i++)}
+                <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                {/for}
             </select>
         </span>
         <em class="t"></em>
@@ -291,6 +297,9 @@
                     <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                     <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                     <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                    {for (var i=0; i<=30; i++)}
+                    <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                    {/for}
                 </select>
             </span>
             <em class="t"></em>
@@ -341,6 +350,9 @@
                 <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                 <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                 <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                {for (var i=0; i<=30; i++)}
+                <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                {/for} 
             </select>
         </span>
         <em class="t"></em>
--- a/usr/lib/lua/luci/view/web/inc/wifi.html
+++ b/usr/lib/lua/luci/view/web/inc/wifi.html
@@ -297,6 +297,9 @@
                 <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                 <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                 <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                {for (var i=0; i<=30; i++)}
+                <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                {/for}
             </select>
         </span>
         <em class="t"></em>
@@ -421,6 +424,9 @@
                 <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                 <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                 <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                {for (var i=0; i<=30; i++)}
+                <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                {/for}
             </select>
         </span>
         <em class="t"></em>
--- a/usr/lib/lua/luci/view/web/setting/wifi.htm
+++ b/usr/lib/lua/luci/view/web/setting/wifi.htm
@@ -222,6 +222,9 @@
                 <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                 <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                 <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                {for (var i=0; i<=30; i++)}
+                <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                {/for}
             </select>
         </span>
         <em class="t"></em>
@@ -347,6 +350,9 @@
                 <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                 <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                 <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                {for (var i=0; i<=30; i++)}
+                <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                {/for}
             </select>
         </span>
         <em class="t"></em>
@@ -482,6 +488,9 @@
                 <option value="max"{if ($txpwr == "max")} selected{/if}><%:穿墙%></option>
                 <option value="mid"{if ($txpwr == "mid")} selected{/if}><%:标准%></option>
                 <option value="min"{if ($txpwr == "min")} selected{/if}><%:节能%></option>
+                {for (var i=0; i<=30; i++)}
+                <option value={i}{if ($txpwr == i)} selected{/if}>{i} dBm ({Math.pow(10, i/10).toFixed(Math.max(0, 3-Math.floor(i/10)))} mW)</option>
+                {/for}
             </select>
         </span>
         <em class="t"></em>
