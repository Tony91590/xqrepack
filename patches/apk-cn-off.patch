--- a/usr/lib/lua/luci/view/web/sysauth.htm
+++ b/usr/lib/lua/luci/view/web/sysauth.htm
@@ -143,9 +143,9 @@
 },
      ccode = '<%=ccode%>',
      hardware = '<%=hardware%>';
 
-    if( ccode == 'CN' && hardware != 'r3a' && hardware != 'r3l' && hardware != 'r4c' && hardware != 'r4cm' && hardware != 'r4ac' && (isMobile.Android() || isMobile.iOS()) )  {
+    if( ccode == 'null' && hardware != 'r3a' && hardware != 'r3l' && hardware != 'r4c' && hardware != 'r4cm' && hardware != 'r4ac' && (isMobile.Android() || isMobile.iOS()) )  {
          $('#admin-mob').show();
 
         if (isMobile.Android()) {
             $('#install').attr('href','http://bigota.miwifi.com/xiaoqiang/client/xqapp_rom.apk');    
